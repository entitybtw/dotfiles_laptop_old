#################
###  imports  ###
#################

source = ~/.cache/wal/colors-hyprland.conf

################
### monitors ###
################

$refreshRate = 60.00

monitor = eDP-1, 1920x1200, 0x0, 1
monitor = HDMI-A-1, 1920x1080@$refreshRateHz, 0x-1080, 1
workspace = 1, monitor:HDMI-A-1
workspace = 6, monitor:eDP-1

###################
###  variables  ###
###################

$terminal = bash $HOME/.config/rofi/scripts/term.sh
$menu = rofi -modi drun,run -show

#################
### autostart ###
#################

exec-once = hyprpm enable split-monitor-workspaces
exec-once = hyprpm enable csgo-vulkan-fix
exec-once = waybar
blurls = waybar
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Adwaita' && gsettings set org.gnome.desktop.interface cursor-size 24
exec-once = netbird up
exec-once = swww-daemon
exec-once = pulseaudio --start
# exec-once = /usr/libexec/polkit-gnome-authentication-agent-1 &
exec-once = rm -rf ~/.config/freerdp/server/*

#####################
### env variables ###
#####################

env = XDG_CURRENT_DESKTOP=xdg-desktop-portal-hyprland
env = XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,30
env = HYPRCURSOR_THEME,Adwaita

###################
### look & feel ###
###################

general {
    gaps_in = 10
    gaps_out = 20

    border_size = 2

    col.active_border = $color1 $color11
    col.inactive_border = rgba(595959aa)

    resize_on_border = false

    allow_tearing = false
    
layout = dwindle
}

decoration {
    rounding = 10

    active_opacity = 1.0
    inactive_opacity = 1.0

    blur {
        enabled = true
        size = 10
        passes = 3
	ignore_opacity = true
        vibrancy = 0.1696
    }
}

  
 animations {
     enabled = true
     bezier = myBezier, 0.05, 0.9, 0.1, 1.05
     animation = windows, 1, 7, myBezier, popin 35%
     animation = windowsOut, 1, 7, myBezier, popin 35%
     animation = border, 1, 10, default
     animation = borderangle, 1, 8, default
     animation = fade, 1, 7, default
     animation = workspaces, 1, 6, myBezier
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    enable_swallow = true
    swallow_regex = ^(Alacritty|kitty|footclient)$
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}



#############
### input ###
#############

input {
    kb_layout = us, ru
    kb_variant =
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
	disable_while_typing = false
    }
}

gestures {
    workspace_swipe = true
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

plugin {
    split-monitor-workspaces {
        count = 5
        keep_focused = 0
        enable_notifications = 0
        enable_persistent_workspaces = 1
    }
    csgo-vulkan-fix {
        res_w = 1024
        res_h = 576

        class = cs2

        fix_mouse = true
    }
    hyprexpo {
        columns = 3
        gap_size = 0
        bg_col = rgb(111111)
        workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1

        enable_gesture = true # laptop touchpad, 4 fingers
        gesture_distance = 300 # how far is the "max"
        gesture_positive = true # positive = swipe down. Negative = swipe up.
    }
}

#############
### binds ###
#############

$mainMod = SUPER
bind = $mainMod SHIFT, A, togglefloating,
bind = $mainMod SHIFT, A, resizeactive, exact 733 480
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod, S, movetoworkspace, +0
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod, S, movetoworkspace, special:magic
bind = $mainMod, S, togglespecialworkspace, magic
bind = ALT, Tab, cyclenext,
bind = SUPER, F, fullscreen
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, V, togglefloating,
bind = $mainMod, D, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, W, exec, bash "$HOME/.config/rofi/scripts/wallpaper_launcher.sh"
bind = $mainMod, J, togglesplit, # dwindle
binde = $mainMod SHIFT, right, resizeactive, 10 0
binde = $mainMod SHIFT, left, resizeactive, -10 0
binde = $mainMod SHIFT, up, resizeactive, 0 -10
binde = $mainMod SHIFT, down, resizeactive, 0 10

bind = CTRL $mainMod, right, movewindow, r
bind = CTRL $mainMod, left, movewindow, l
bind = CTRL $mainMod, up, movewindow, u
bind = CTRL $mainMod, down, movewindow, d
bind = CTRL ALT, Q, exec, sudo vpn toggle | xargs -I{} notify-send "{}"

bind = ALT, Z, exec, bash $HOME/.config/rofi/scripts/rofi-screenshot.sh

bind = ALT CTRL, Z, exec, hyprshot -m region --clipboard-only

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, 1, split-workspace, 1
bind = $mainMod, 2, split-workspace, 2
bind = $mainMod, 3, split-workspace, 3
bind = $mainMod, 4, split-workspace, 4
bind = $mainMod, 5, split-workspace, 5

bind = $mainMod SHIFT, 1, split-movetoworkspace, 1
bind = $mainMod SHIFT, 2, split-movetoworkspace, 2
bind = $mainMod SHIFT, 3, split-movetoworkspace, 3
bind = $mainMod SHIFT, 4, split-movetoworkspace, 4
bind = $mainMod SHIFT, 5, split-movetoworkspace, 5

bind = $mainMod, A, movewindow,mon:+1

bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bindel = ,XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -1%
bindel = ,XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +1%
bindel = ,XF86AudioMute, exec, bash $HOME/.config/rofi/scripts/navigation.sh

bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

############################
### windows & workspaces ###
############################

windowrulev2 = opacity 0.7, 0.7, class:(Rofi)
windowrulev2 = opacity 0.7, 0.7, class:(thunderbird)
windowrulev2 = opacity 0.99999, 0.99999, class:(zen)
windowrulev2 = opacity 0.7, 0.7, class:(hydralauncher)
windowrulev2 = opacity 0.7, 0.7, class:(vesktop)
windowrulev2 = opacity 0.8, 0.8,class:(org.gnome.Nautilus)
windowrulev2 = opacity 0.7, 0.7,class:(com.ayugram.desktop)
windowrulev2 = opacity 0.8, 0.8,class:(Code)
windowrulev2 = opacity 0.8, 0.8,class:(obsidian)
# windowrulev2 = opacity 0.8, 0.8,class:(obsidian)
windowrulev2 = tile, class:^(cs2)$
windowrulev2 = tile, class:^(Brave-browser)$
windowrulev2 = tile, class:^(ppssppwindows64.exe)$
windowrulev2 = tile, class:^(epicgameslauncher.exe)$
windowrulev2 = tile, class:^(kitty)$
windowrulev2 = noblur, class:^(FreeRDP)$
# windowrulev2 = minsize 10 10,class:^FreeRDP,title:^$

windowrulev2 = suppressevent maximize, class:.*

windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
